# UI authority chain

Frozen logic SHA → owner-supplied Sage Station screen-pack hash → `APP_UI_CODEGEN_CONTRACT.yaml` → SwiftUI production source.

The iOS screens use the same owner-directed composition and semantic identity as Android, with native SwiftUI controls and the iOS-specific one-time remove-ads business rule.
